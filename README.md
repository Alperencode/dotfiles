# Dotfiles

To download and run the setup workspace script, run the following command.

```bash
git clone https://github.com/Alperencode/dotfiles.git \
cd dotfiles \
chown +x setup_workspace.sh \
./setup_workspace.sh
````
